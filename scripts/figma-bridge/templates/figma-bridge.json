{
  "$comment": "Figma Bridge config. The one file that makes the generic checks repo-specific — see tools/figma-bridge for what reads it. Change figma.fileKey with `figma-bridge retarget`, never by hand: the key also appears in every mapping and every component doc-link, and a partial rewrite publishes a mixture.",
  "designSystem": {
    "name": "REPLACE — the library's name as designers say it",
    "roots": ["src/design-system"],
    "ignoreDirs": ["icons"],
    "componentExtension": ".tsx",
    "mappingSuffix": ".figma.ts",
    "draftSuffix": ".figma.tsx"
  },
  "figma": {
    "fileKey": "REPLACE",
    "fileName": "REPLACE — the file name as it reads in Figma",
    "retiredPagePattern": "graveyard|deprecated|archive"
  },
  "codeConnect": {
    "parser": "react",
    "label": "REPLACE — the label Dev Mode shows above the snippet, e.g. React Native"
  },
  "paths": {
    "unmapped": "design/figma/unmapped-components.json",
    "componentCache": "design/figma/components.json",
    "componentCacheMeta": "design/figma/components.meta.json",
    "tokenMap": "design/figma/tokens.md",
    "libraryNotes": "design/figma/library-notes.md"
  },
  "verify": ["npm run type-check", "npm run lint", "npm run test:unit"],
  "fonts": {
    "families": [],
    "source": null,
    "substitute": { "family": "Inter", "style": "SemiBold" }
  }
}
