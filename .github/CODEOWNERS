# Every pull request needs an approving review from an owner here, enforced by
# the branch protection on main.
#
# One owner, deliberately. The consequence is that admin enforcement has to stay
# off: GitHub never counts a pull request's author as its approver, so a sole
# code owner cannot approve their own work and would otherwise be unable to merge
# anything at all. The bypass is what keeps that possible — see README.
*   @wmcheung
